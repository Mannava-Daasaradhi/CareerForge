{
  "name": "careerforge-e2e",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:backend": "playwright test --grep 'Backend Health'",
    "test:auth": "playwright test --grep 'Authentication'",
    "test:pages": "playwright test --grep 'Page Loads'",
    "test:features": "playwright test --grep 'Feature'",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0"
  }
}
